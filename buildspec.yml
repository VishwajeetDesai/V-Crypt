version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
artifacts:
  files:
     - '.android/app/build/outputs/apk/debug/app-debug.apk'
  discard-paths: yes